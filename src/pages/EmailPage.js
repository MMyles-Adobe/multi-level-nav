import React from 'react';
import { View, Heading, Text } from '@adobe/react-spectrum';

const EmailPage = () => {
  return (
    <View>
      <Heading level={1}>Email</Heading>
      <Text>Email configuration page</Text>
    </View>
  );
};

export default EmailPage; 